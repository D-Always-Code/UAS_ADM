# Layanan database
services:
  db-webdinamis:
    image: mariadb:lts
    environment:
      MARIADB_ROOT_PASSWORD: password!123
      MARIADB_DATABASE: uas_adm_doni
      MARIADB_USER: uas_doni_2388010045
      MARIADB_PASSWORD: Setiawan5525!
    volumes:
      - db_data:/var/lib/mysql
      - ./db_data/uas_adm_doni.sql:/docker-entrypoint-initdb.d/uas_adm_doni.sql:ro
    restart: always

  container-statis:
    image: donisetiawan25/uas-doni:latest
    ports:
      - "80:80"
    restart: always

  container-dinamis:
    image: donisetiawan25/uas-doni:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      DB_USER: uas_doni_2388010045
      DB_PASSWORD: Setiawan5525!
      DB_NAME: uas_adm_doni
      DB_HOST: db-webdinamis
      NEXTAUTH_SECRET: ini-kata-kata-bebas-saja-32-karekter
      NEXTAUTH_URL: http://3.25.181.208:3000
    depends_on:
      - db-webdinamis

volumes:
  db_data: {}
